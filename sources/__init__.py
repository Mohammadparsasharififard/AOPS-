"""Source package — per-source adapters and authorization configs.

Each source has its own subdirectory with:
- source.yaml — configuration + authorization scope
- adapter.py — source-specific parsing (optional; generic fallback exists)
- README.md — what's covered, what's not, what's never done
"""
